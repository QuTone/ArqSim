# Core dependencies for the ArqSim HTTP server

# ArqSim core (`arqsim`) — editable install from the monorepo root.
# setup.sh runs pip from this server/ directory, so `..` resolves to the
# repository root.
-e ..

# Web API
fastapi>=0.100.0
pydantic>=2.0
uvicorn>=0.20.0
